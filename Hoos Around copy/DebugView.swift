//import SwiftUI
//import FirebaseFunctions
//import FirebaseAuth   // 👈 add this

//struct DebugView: View {
//    var body: some View {
 ///       Text("Check Egress IP")
//            .onAppear {
                // 👇 force sign out so no stale token is used
//                try? Auth.auth().signOut()

//                let functions = Functions.functions(region: "us-central1")
//                functions.httpsCallable("checkEgressIp").call { result, error in
                 //   if let error = error {
//                        print("❌", error)
                  //  } else if let data = result?.data as? [String: Any],
                    //          let ip = data["ip"] as? String {
 //                       print("🌐 Egress IP:", ip)
               //     }
//                }
     //       }
   // }
//}
